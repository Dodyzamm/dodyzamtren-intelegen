import React, { useState, useEffect } from 'react';\n\nconst ToDoList = () => {\n    const [tasks, setTasks] = useState([]);\n    const [taskInput, setTaskInput] = useState('');\n    const [editIndex, setEditIndex] = useState(null);\n\n    useEffect(() => {\n        const storedTasks = JSON.parse(localStorage.getItem('tasks')) || [];\n        setTasks(storedTasks);\n    }, []);\n\n    useEffect(() => {\n        localStorage.setItem('tasks', JSON.stringify(tasks));\n    }, [tasks]);\n\n    const addTask = () => {\n        if (taskInput.trim() === '') return;\n        const newTasks = [...tasks];\n        if (editIndex !== null) {\n            newTasks[editIndex] = { text: taskInput, completed: false };\n            setEditIndex(null);\n        } else {\n            newTasks.push({ text: taskInput, completed: false });\n        }\n        setTasks(newTasks);\n        setTaskInput('');\n    };\n\n    const deleteTask = (index) => {\n        const newTasks = tasks.filter((_, i) => i !== index);\n        setTasks(newTasks);\n    };\n\n    const toggleComplete = (index) => {\n        const newTasks = tasks.map((task, i) => (\n            i === index ? { ...task, completed: !task.completed } : task\n        ));\n        setTasks(newTasks);\n    };\n\n    const editTask = (index) => {\n        setTaskInput(tasks[index].text);\n        setEditIndex(index);\n    };\n\n    return (\n        <div className="container mx-auto mt-10">\n            <h1 className="text-2xl font-bold mb-4">To-Do List</h1>\n            <div className="mb-4">\n                <input\n                    type="text"\n                    value={taskInput}\n                    onChange={(e) => setTaskInput(e.target.value)}\n                    className="border border-gray-400 p-2 rounded-lg w-full"\n                    placeholder="Add a new task..."\n                />\n                <button\n                    onClick={addTask}\n                    className="bg-blue-500 text-white p-2 rounded-lg mt-2"\n                >\n                    {editIndex !== null ? 'Edit Task' : 'Add Task'}\n                </button>\n            </div>\n            <ul className="list-disc pl-5">\n                {tasks.map((task, index) => (\n                    <li key={index} className="flex justify-between items-center mb-2">\n                        <span className={task.completed ? 'line-through' : ''}> {task.text}</span>\n                        <div>\n                            <button onClick={() => toggleComplete(index)} className="bg-green-500 text-white p-1 rounded-lg mx-1">\n                                {task.completed ? 'Undo' : 'Complete'}\n                            </button>\n                            <button onClick={() => editTask(index)} className="bg-yellow-500 text-white p-1 rounded-lg mx-1">Edit</button>\n                            <button onClick={() => deleteTask(index)} className="bg-red-500 text-white p-1 rounded-lg mx-1">Delete</button>\n                        </div>\n                    </li>\n                ))}\n            </ul>\n        </div>\n    );\n};\n\nexport default ToDoList;